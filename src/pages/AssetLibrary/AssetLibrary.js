import { Link } from 'react-router-dom';
import { Button, Space } from 'antd';

export const AssetLibrary = () => {
    return (
        <div className="asset-library">
            {/* <Link to="/resources/add">
                <Button type="primary">Add New Resource</Button>
            </Link> */}
            <h1>Assets</h1>
        </div>

    );
}